const initialState = {
	isBusy: false,
	error: false
};

const homeReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default homeReducer;
